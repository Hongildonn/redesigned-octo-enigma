/**
 * Main Application Entry Point
 * 
 * This is a basic JavaScript starter template.
 * Modify this file to add your application logic.
 */

// Example: Simple function
function greet(name) {
  return `Hello, ${name}!`;
}

// Example: Execute function
console.log(greet('World'));

// Example: Export for use in other modules (if using ES modules)
// export { greet };

// Example: CommonJS export (if using Node.js)
// module.exports = { greet };
